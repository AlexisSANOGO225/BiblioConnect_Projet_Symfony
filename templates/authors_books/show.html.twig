{% extends 'base.html.twig' %}

{% block title %}AuthorsBooks{% endblock %}

{% block body %}
    <h1>AuthorsBooks</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ authors_book.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_authors_books_index') }}">back to list</a>

    <a href="{{ path('app_authors_books_edit', {'id': authors_book.id}) }}">edit</a>

    {{ include('authors_books/_delete_form.html.twig') }}
{% endblock %}
